[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "node index.js"
